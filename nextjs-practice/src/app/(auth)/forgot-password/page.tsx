// (auth) is made to group the routes together , we can directly access the routes by visiting /login,/register, /forgot-password
export default function ForgotPassword() {
  return (
    <div>Forgot Password</div>
  )
}